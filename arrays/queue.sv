  module tb;
  ////////////////////////QUEUE//////////////////////

 /* int q[$];

  initial begin
    q='{10,20,30,40,50};

    foreach(q[i])begin
      $display("q[%0d]=%0d",i,q[i]);
  end
    $display("queue size : %0d",q.size());

    q.push_front(1);
    $display("after push_front queue: %p",q);

    q.push_back(60);
    $display("after push_back queue: %p",q);

    q.pop_back();
    $display("after pop_back queue: %p",q);

    q.pop_front();
    $display("after pop_front queue: %p",q);

    q.insert(2,25);
    $display("after insert queue : %p",q);

    q.delete(4);
    $display("after delete one index queue: %p",q);

    q.delete();
    $display("after delete queue: %p",q);


  end*/

endmodule

